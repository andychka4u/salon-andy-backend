services:
  - type: web
    name: andy-peerjs
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PEER_MOUNT
        value: /peerjs
      - key: PEER_API_PATH
        value: /
      - key: CORS_ORIGIN
        value: "*"
